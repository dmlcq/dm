# 食品配料表AI分析 - 设计指南

## 气质与意象

**关键词**: 清新、健康、自然、信任、专业

**具象场景**: 春日清晨的有机农场，阳光透过嫩绿叶片洒落，空气中弥漫着泥土与青草的清新气息。简洁的白底包装，配料一目了然，无需担忧隐藏风险。

## 视觉策略

- **图形语言**: 圆润卡片、柔和阴影、轻微圆角，避免尖锐边缘
- **状态标识**: 用颜色区分安全(绿)、警示(橙)、高风险(红)三级
- **图标风格**: Lucide 简洁线性图标，strokeWidth 1.5-2

## 配色方案

| 用途 | 色值 | 意象来源 | Tailwind类 |
|------|------|----------|------------|
| 主色 | #22c55e | 嫩叶新芽 | `bg-green-500` |
| 主色浅 | #86efac | 晨光透过叶片 | `bg-green-300` |
| 主色深 | #15803d | 深绿茎杆 | `bg-green-700` |
| 安全色 | #22c55e | 健康食品标识 | `text-green-500` |
| 警示色 | #f97316 | 需注意配料 | `text-orange-500` |
| 高风险色 | #ef4444 | 添加剂警告 | `text-red-500` |
| 背景 | #f0fdf4 | 清晨薄雾 | `bg-green-50` |
| 卡片背景 | #ffffff | 纯净白纸 | `bg-white` |
| 文字主色 | #1f2937 | 墨色印刷 | `text-gray-800` |
| 文字辅色 | #6b7280 | 说明文字 | `text-gray-500` |

## 字体排版

- **标题**: 系统默认字体，粗体，18-24px
- **配料名称**: 中等粗细，14-16px
- **说明文字**: 常规字重，12-14px
- **数字评分**: 粗体，32px 用于健康评分

## 间距系统

- **页面边距**: `p-4` (16px)
- **卡片内边距**: `p-4` (16px)
- **卡片间距**: `gap-3` (12px)
- **组件间距**: `gap-2` (8px)

## 组件使用原则

- **通用组件优先使用 `@/components/ui/*`**：
  - 按钮：`Button`（拍照、上传、重新分析）
  - 卡片：`Card`（配料卡片、评分卡片）
  - 标签：`Badge`（风险等级标签）
  - 进度条：`Progress`（健康评分环形）
  - 提示：`Toast`（分析完成、错误提示）
  - 分隔线：`Separator`（卡片分隔）
  - 骨架屏：`Skeleton`（加载态）

## 导航结构

单页面应用，无 TabBar：
- 首页入口：拍照/选图 → 分析 → 结果展示
- 页面内滚动查看所有配料详情

## 页面布局

### 首页结构
```
┌─────────────────────────────┐
│         顶部标题区           │
│   "配料表AI分析" + 品牌绿色   │
├─────────────────────────────┤
│         操作入口区           │
│   [拍照] [从相册选择] 按钮    │
├─────────────────────────────┤
│        图片预览区            │
│   选择的配料表图片缩略       │
├─────────────────────────────┤
│        健康评分区            │
│   环形进度 + 评分数字 + 建议 │
├─────────────────────────────┤
│        配料列表区            │
│   卡片式展示每个配料详情     │
│   - 配料名称                 │
│   - 风险等级标签             │
│   - 安全说明                 │
│   - 替代建议                 │
└─────────────────────────────┘
```

## 设计禁忌

- ❌ 不要使用深色/黑色背景（破坏清新感）
- ❌ 不要使用过多渐变或复杂装饰
- ❌ 不要用尖锐三角形或硬边设计
- ❌ 不要堆砌信息，保持卡片内容精简
- ❌ 不要用小字号展示重要配料名称
- ❌ 不要忽略加载态和空状态设计

## 状态展示

- **加载态**: 使用 Skeleton 骨架屏，卡片式灰色占位
- **空状态**: "请上传配料表图片" + 拍照图标引导
- **错误态**: Toast 提示 + 简短错误说明

## 小程序约束

- 图片上传使用 `Network.uploadFile`
- 后端使用 `S3Storage` 存储图片
- AI 分析使用 `LLMClient` 多模态能力
- 响应需控制在合理时间（提示加载态）